subinclude("//build/rules/js:defs")

pnpm_script(
    name = "dist",
    srcs = [
        "package.json",
        "src",
        "tsconfig.json",
    ],
    outs = ["dist"],
    pnpm_deps = "//:pnpm",
    script_name = "build",
    visibility = ["//frontend/..."],
)

filegroup(
    name = "shared",
    srcs = [
        "package.json",
        ":dist",
    ],
    visibility = ["//frontend/..."],
)
