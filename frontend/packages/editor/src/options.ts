import {PARAGRAPH_OPTIONS, HEADING_OPTIONS} from './elements'
import {BOLD_OPTIONS} from './marks'

export const options = {
  ...PARAGRAPH_OPTIONS,
  ...HEADING_OPTIONS,
  //marks
  ...BOLD_OPTIONS,
}
