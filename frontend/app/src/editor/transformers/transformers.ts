import * as documents from '@mintter/api/documents/v1alpha/documents_pb';
export function publicationToEditor(document: documents.Document.AsObject) {
  console.log(
    '🚀 ~ file: transformers.ts ~ line 3 ~ publicationToEditor ~ document',
    document,
  );
  return null;
}
