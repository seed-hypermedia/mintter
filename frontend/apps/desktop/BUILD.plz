subinclude("//build/rules/js:defs")

yarn_script(
    name = "dist",
    srcs = [
        "index.html",
        "package.json",
        "public",
        "src",
        "tsconfig.json",
        "vite.config.ts",
    ],
    outs = ["dist"],
    script_name = "build",
    yarn_deps = "//:yarn",
)
