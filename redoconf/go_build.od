rc_include _all.rc

cat >$3 <<EOF
#!/bin/sh -e
GOOS=$(shquote "$GOOS")
GOARCH=$(shquote "$GOARCH")
CGO_ENABLED=$(shquote "$CGO_ENABLED")

echo "Building Go binary for \$GOOS/\$GOARCH..." >&2
GOOS=$GOOS GOARCH=$GOARCH CGO_ENABLED=$CGO_ENABLED go build \$@
EOF
chmod a+x $3
redo-stamp <$3
