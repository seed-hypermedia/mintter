load("//build:rules.bzl", "executable")

package(default_visibility = ["//visibility:public"])

executable(
    name = "rice",
    srcs = [
        "//:go.mod",
        "//:go.sum",
    ],
    cmd = "cd $SOURCE_ROOT_DIR && go build -o $TARGET_OUT_DIR/rice github.com/GeertJohan/go.rice/rice",
)
